# Environment (never commit secrets)
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
.venv/
venv/
*.egg-info/
.eggs/
dist/
build/
*.egg
*.log
*.pid
.superpowers/

# Local agent / MCP caches (do not commit)
.playwright-mcp/
.claude/
.trae/

# Coverage / test artifacts
.coverage
.coverage.*
htmlcov/
coverage.xml
*.cover

# Database & SQLite sidecars (local `data/aitext.db` etc.)
*.db
*.db.backup
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# App data dir (`application.paths.DATA_DIR` = repo `data/`)
# — runtime file storage (cast, foreshadowings, legacy chapter files, etc.)
data/novels/
data/foreshadowings/

# Generated bible JSON snapshots under data/bibles/
data/bibles/bible-novel-*.json
data/bibles/novel-*-bible.json
data/bibles/bible-test-*.json
data/bibles/bible-final-*.json

# Book output (keep placeholder only)
output/novels/*/
!output/novels/.gitkeep

# Temporary / generated API fixtures
interfaces/data/


# OS / editors
.DS_Store
Thumbs.db
Desktop.ini

# Local vector DB / dev dumps (if checked in by mistake)
qdrant_storage/
.models/
frontend/node_modules/

# Evaluation results
scripts/evaluation/results/

# Local runtime environment artifacts
# 这些是本机启动、索引、日志、模型下载产生的内容，不应入库
data/chromadb/
data/qdrant_storage/
data/logs/
data/llm_configs.json
logs/
data/qdrant/


PlotPilot-master

llm_profiles.json
